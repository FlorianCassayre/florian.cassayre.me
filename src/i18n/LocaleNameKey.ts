import { Locale } from './Locale';
import { I18nMessageIds } from './messages';

export const LocaleNameKey: Record<Locale, I18nMessageIds> = {
  [Locale.English]: 'language.english',
  [Locale.French]: 'language.french',
};
