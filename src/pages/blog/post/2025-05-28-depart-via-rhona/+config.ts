import { PostConfig } from '../../../../blog/PostConfig';
import { Locale } from '../../../../i18n/Locale';

export const config = {
  post: {
    title: 'ViaRhôna : départ',
    description: 'Début du voyage sur la ViaRhôna.',
    locale: Locale.French,
    keywords: ['personal', 'cycling'],
  },
} satisfies PostConfig;
