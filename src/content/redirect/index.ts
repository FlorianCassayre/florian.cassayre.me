import { INVISIBLE_REDIRECTIONS } from './legacy';
import { REDIRECTIONS } from './redirections';

export const REDIRECT_PAGE_NAME = '_redirect';

export const ALL_REDIRECTIONS = { ...INVISIBLE_REDIRECTIONS, ...REDIRECTIONS };
